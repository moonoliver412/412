# CodeSprout cloud features (optional — the app is fully functional without).
# 1. Create a Supabase project, run supabase/schema.sql in its SQL editor.
# 2. Auth settings: enable anonymous sign-ins + manual identity linking.
# 3. Copy this file to .env.local and fill in:
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
